$(document).ready(function(){		
	$('select').chosen({
		no_results_text: "Sorry, we don't have any available devices at the time you selected.",
		placeholder_text_multiple: "Pick some Devices"
	});
});
